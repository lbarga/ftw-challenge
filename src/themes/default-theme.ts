import { createTheme } from "@mui/material";

export const defaultTheme = createTheme({
  palette: {
    primary: {
      main: "#FF9A45",
    },
    secondary: {
      main: "#FF7E3A",
    },
  },
});
